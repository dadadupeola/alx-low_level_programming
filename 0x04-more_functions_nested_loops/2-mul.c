#include "main.h"


/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with firs number
 * Return: The result of multiplication between a person add y person
 */

int mul(int x, int y)

{
	return (x * y);
}
